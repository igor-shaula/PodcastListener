<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <size
        android:width="@dimen/defaultPictureSize"
        android:height="@dimen/defaultPictureSize" />

    <stroke
        android:width="@dimen/size_2"
        android:color="@color/accent" />

    <gradient
        android:gradientRadius="100px"
        android:startColor="@color/primary_light"
        android:endColor="@color/accent"
        android:type="radial" />

</shape>